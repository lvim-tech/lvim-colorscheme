# lvim-colorscheme — colours only, as a drop-in.
#
# dunst reads ~/.config/dunst/dunstrc first and this file after it, so what is set here wins.
# Geometry, timeouts, font and rules stay in dunstrc and are not this file's business.
#
# Quoted because '#' starts a comment in dunst's config unless the value is a string.

[global]
    separator_color = frame

[urgency_low]
    background = "#273339"
    foreground = "#b2c7c7"
    frame_color = "#273339"
    highlight = "#cfa96a"

[urgency_normal]
    background = "#273339"
    foreground = "#b2c7c7"
    frame_color = "#273339"
    highlight = "#df8064"

[urgency_critical]
    background = "#273339"
    foreground = "#b2c7c7"
    frame_color = "#273339"
    highlight = "#de7e82"

# The title, coloured by urgency. These are rules rather than urgency sections because `format` is
# a rule attribute; the filter is what an urgency section would have had implied.

[lvim_title_low]
    msg_urgency = low
    format = "<span foreground='#cfa96a'><b>%s</b></span>\n%b"

[lvim_title_normal]
    msg_urgency = normal
    format = "<span foreground='#df8064'><b>%s</b></span>\n%b"

[lvim_title_critical]
    msg_urgency = critical
    format = "<span foreground='#de7e82'><b>%s</b></span>\n%b"
