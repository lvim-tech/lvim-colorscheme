#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme RosepineSoft
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b3afb2", italic = true }

# Find
find_keyword  = { fg = "#1f1d22", bg = "#a98a89", bold = true }
find_position = { fg = "#b9b4b5", bg = "#28272d", bold = true }

# Symlink
symlink_target = { fg = "#896882", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#ac9682", bg = "#ac9682" }
marker_cut      = { fg = "#a0727c", bg = "#a0727c" }
marker_marked   = { fg = "#9084a2", bg = "#9084a2" }
marker_selected = { fg = "#5d6d74", bg = "#5d6d74" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#fdfcfe", bg = "#697d6d" }
count_cut      = { fg = "#f8f6fd", bg = "#95646f" }
count_selected = { fg = "#dbd9d9", bg = "#51635d" }

# Border
border_symbol = "│"
border_style  = { fg = "#5b6b5f" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#302d35" }
current = { bg = "#35313b" }
preview = { bg = "#35313b" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b9b4b5", bg = "#252328", bold = true }
inactive = { fg = "#979295", bg = "#232126" }

[mode]
normal_main = { fg = "#efe9f9", bg = "#5d6d74", bold = true }
normal_alt  = { fg = "#84a9b8", bg = "#3c3742" }

select_main = { fg = "#1f1d22", bg = "#9084a2", bold = true }
select_alt  = { fg = "#aa9fba", bg = "#3c3742" }

unset_main  = { fg = "#f8f6fd", bg = "#726f7c", bold = true }
unset_alt   = { fg = "#a3a0ad", bg = "#3c3742" }

[status]
# Progress
progress_label  = { fg = "#b3afb2", bold = true }
progress_normal = { fg = "#b9b4b5", bg = "#1f1d22" }
progress_error  = { fg = "#fdfcfe", bg = "#a0727c" }

# Permissions
perm_type  = { fg = "#738d98" }
perm_read  = { fg = "#b6a18b" }
perm_write = { fg = "#ab7c86" }
perm_exec  = { fg = "#7a8e7e" }
perm_sep   = { fg = "#896882" }

[pick]
border   = { fg = "#5d7078" }
active   = { fg = "#b9b4b5", bg = "#252328" }
inactive = { fg = "#b3afb2" }

[input]
border   = { fg = "#5d7078" }
title    = { fg = "#5d7078" }
value    = { fg = "#8b8896" }
selected = { fg = "#b9b4b5", bg = "#252328" }

[cmp]
border   = { fg = "#5d7078" }
active   = { fg = "#b9b4b5", bg = "#252328" }
inactive = { fg = "#b3afb2" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#5b6b5f" }
title   = { fg = "#b3afb2" }
hovered = { fg = "#b9b4b5", bg = "#252328" }

[which]
cols            = 3
mask            = { bg = "#1f1d22" }
cand            = { fg = "#7a8a8a" }
rest            = { fg = "#718a95" }
desc            = { fg = "#9084a2" }
separator       = " ➜ "
separator_style = { fg = "#896882" }

[confirm]
border  = { fg = "#5d7078" }
title   = { fg = "#5d7078" }
body    = {}
list    = {}
btn_yes = { fg = "#b9b4b5", bg = "#252328" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#5b6b5f" }
title    = { fg = "#b3afb2" }
tbl_col  = { fg = "#738d98" }
tbl_cell = { fg = "#b6a18b", bg = "#252328" }

[notify]
title_info  = { fg = "#6f8e9a" }
title_warn  = { fg = "#a48180" }
title_error = { fg = "#b67a88" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#7a8e7e" }
run     = { fg = "#9084a2" }
desc    = { fg = "#979295" }
hovered = { fg = "#b9b4b5", bg = "#252328", bold = true }
footer  = { fg = "#b9b4b5", bg = "#252328" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#b6a18b" },

	# Media
	{ mime = "{audio,video}/*", fg = "#9084a2" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#ab7c86" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#7c8c8c" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#896882" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#a0727c" },
	{ url = "*", is = "exec"  , fg = "#7a8e7e" },
	{ url = "*", is = "dummy", bg = "#a0727c" },
	{ url = "*/", is = "dummy", bg = "#a0727c" },

	# Fallback
	{ url = "*/", fg = "#738d98" }
]
    