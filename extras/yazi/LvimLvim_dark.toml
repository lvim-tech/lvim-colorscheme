#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme LvimDark
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#639696", italic = true }

# Find
find_keyword  = { fg = "#1c2327", bg = "#e19556", bold = true }
find_position = { fg = "#55a6ab", bg = "#232e35", bold = true }

# Symlink
symlink_target = { fg = "#577576", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#d1aa64", bg = "#d1aa64" }
marker_cut      = { fg = "#d75e52", bg = "#d75e52" }
marker_marked   = { fg = "#c6614d", bg = "#c6614d" }
marker_selected = { fg = "#447ca2", bg = "#447ca2" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#fafdff", bg = "#718650" }
count_cut      = { fg = "#fafdff", bg = "#cc5147" }
count_selected = { fg = "#bae7ff", bg = "#366c5f" }

# Border
border_symbol = "│"
border_style  = { fg = "#617349" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#283238" }
current = { bg = "#2b3940" }
preview = { bg = "#2b3940" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#55a6ab", bg = "#22292d", bold = true }
inactive = { fg = "#39858a", bg = "#20272b" }

[mode]
normal_main = { fg = "#fafdff", bg = "#447ca2", bold = true }
normal_alt  = { fg = "#15b0f4", bg = "#2f3d45" }

select_main = { fg = "#fafdff", bg = "#c6614d", bold = true }
select_alt  = { fg = "#f18978", bg = "#2f3d45" }

unset_main  = { fg = "#e2f4ff", bg = "#40719a", bold = true }
unset_alt   = { fg = "#1caeff", bg = "#2f3d45" }

[status]
# Progress
progress_label  = { fg = "#639696", bold = true }
progress_normal = { fg = "#55a6ab", bg = "#1c2327" }
progress_error  = { fg = "#fafdff", bg = "#d75e52" }

# Permissions
perm_type  = { fg = "#3d95c9" }
perm_read  = { fg = "#d8b36f" }
perm_write = { fg = "#e2695e" }
perm_exec  = { fg = "#7e965a" }
perm_sep   = { fg = "#577576" }

[pick]
border   = { fg = "#3a7599" }
active   = { fg = "#55a6ab", bg = "#22292d" }
inactive = { fg = "#639696" }

[input]
border   = { fg = "#3a7599" }
title    = { fg = "#3a7599" }
value    = { fg = "#3095d7" }
selected = { fg = "#55a6ab", bg = "#22292d" }

[cmp]
border   = { fg = "#3a7599" }
active   = { fg = "#55a6ab", bg = "#22292d" }
inactive = { fg = "#639696" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#617349" }
title   = { fg = "#639696" }
hovered = { fg = "#55a6ab", bg = "#22292d" }

[which]
cols            = 3
mask            = { bg = "#1c2327" }
cand            = { fg = "#4194a7" }
rest            = { fg = "#4090c0" }
desc            = { fg = "#da664e" }
separator       = " ➜ "
separator_style = { fg = "#567273" }

[confirm]
border  = { fg = "#3a7599" }
title   = { fg = "#3a7599" }
body    = {}
list    = {}
btn_yes = { fg = "#55a6ab", bg = "#22292d" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#617349" }
title    = { fg = "#639696" }
tbl_col  = { fg = "#3d95c9" }
tbl_cell = { fg = "#d8b36f", bg = "#22292d" }

[notify]
title_info  = { fg = "#1b97cf" }
title_warn  = { fg = "#da8950" }
title_error = { fg = "#eb6157" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#7e965a" }
run     = { fg = "#e4694e" }
desc    = { fg = "#39858a" }
hovered = { fg = "#55a6ab", bg = "#22292d", bold = true }
footer  = { fg = "#55a6ab", bg = "#22292d" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#d8b36f" },

	# Media
	{ mime = "{audio,video}/*", fg = "#e4694e" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#e2695e" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#3f9aae" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#577576" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#d75e52" },
	{ url = "*", is = "exec"  , fg = "#7e965a" },
	{ url = "*", is = "dummy", bg = "#d75e52" },
	{ url = "*/", is = "dummy", bg = "#d75e52" },

	# Fallback
	{ url = "*/", fg = "#3d95c9" }
]
    