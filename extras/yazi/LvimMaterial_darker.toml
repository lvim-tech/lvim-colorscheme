#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme MaterialDarker
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b2c7c7", italic = true }

# Find
find_keyword  = { fg = "#182125", bg = "#d37a60", bold = true }
find_position = { fg = "#b8bec2", bg = "#212c38", bold = true }

# Symlink
symlink_target = { fg = "#626f76", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#c79e5e", bg = "#c79e5e" }
marker_cut      = { fg = "#d05e64", bg = "#d05e64" }
marker_marked   = { fg = "#d75d70", bg = "#d75d70" }
marker_selected = { fg = "#5b7fcb", bg = "#5b7fcb" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#182125", bg = "#83a15c" }
count_cut      = { fg = "#f9fdff", bg = "#c34e54" }
count_selected = { fg = "#182125", bg = "#5bac96" }

# Border
border_symbol = "│"
border_style  = { fg = "#6e8852" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#233239" }
current = { bg = "#25373f" }
preview = { bg = "#25373f" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b8bec2", bg = "#1e272b", bold = true }
inactive = { fg = "#bbc8cd", bg = "#1c2529" }

[mode]
normal_main = { fg = "#f9fdff", bg = "#5b7fcb", bold = true }
normal_alt  = { fg = "#83a1ea", bg = "#283c43" }

select_main = { fg = "#f9fdff", bg = "#d75d70", bold = true }
select_alt  = { fg = "#e98995", bg = "#283c43" }

unset_main  = { fg = "#f9fdff", bg = "#8f74a3", bold = true }
unset_alt   = { fg = "#b29ac5", bg = "#283c43" }

[status]
# Progress
progress_label  = { fg = "#b2c7c7", bold = true }
progress_normal = { fg = "#b8bec2", bg = "#182125" }
progress_error  = { fg = "#f9fdff", bg = "#d05e64" }

# Permissions
perm_type  = { fg = "#6589d8" }
perm_read  = { fg = "#cfa96a" }
perm_write = { fg = "#d5696e" }
perm_exec  = { fg = "#90ad68" }
perm_sep   = { fg = "#626f76" }

[pick]
border   = { fg = "#4f6fbb" }
active   = { fg = "#b8bec2", bg = "#1e272b" }
inactive = { fg = "#b2c7c7" }

[input]
border   = { fg = "#4f6fbb" }
title    = { fg = "#4f6fbb" }
value    = { fg = "#9c81b0" }
selected = { fg = "#b8bec2", bg = "#1e272b" }

[cmp]
border   = { fg = "#4f6fbb" }
active   = { fg = "#b8bec2", bg = "#1e272b" }
inactive = { fg = "#b2c7c7" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#6e8852" }
title   = { fg = "#b2c7c7" }
hovered = { fg = "#b8bec2", bg = "#1e272b" }

[which]
cols            = 3
mask            = { bg = "#182125" }
cand            = { fg = "#65a1bd" }
rest            = { fg = "#6187d6" }
desc            = { fg = "#d96173" }
separator       = " ➜ "
separator_style = { fg = "#606d73" }

[confirm]
border  = { fg = "#4f6fbb" }
title   = { fg = "#4f6fbb" }
body    = {}
list    = {}
btn_yes = { fg = "#b8bec2", bg = "#1e272b" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#6e8852" }
title    = { fg = "#b2c7c7" }
tbl_col  = { fg = "#6589d8" }
tbl_cell = { fg = "#cfa96a", bg = "#1e272b" }

[notify]
title_info  = { fg = "#6188e4" }
title_warn  = { fg = "#d57053" }
title_error = { fg = "#e1646a" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#90ad68" }
run     = { fg = "#da6576" }
desc    = { fg = "#bbc8cd" }
hovered = { fg = "#b8bec2", bg = "#1e272b", bold = true }
footer  = { fg = "#b8bec2", bg = "#1e272b" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#cfa96a" },

	# Media
	{ mime = "{audio,video}/*", fg = "#da6576" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#d5696e" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#65a1bd" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#626f76" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#d05e64" },
	{ url = "*", is = "exec"  , fg = "#90ad68" },
	{ url = "*", is = "dummy", bg = "#d05e64" },
	{ url = "*/", is = "dummy", bg = "#d05e64" },

	# Fallback
	{ url = "*/", fg = "#6589d8" }
]
    