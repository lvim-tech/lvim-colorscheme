#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme CatppuccinSoft
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b2b7c7", italic = true }

# Find
find_keyword  = { fg = "#1e1f29", bg = "#b88970", bold = true }
find_position = { fg = "#b2b7c7", bg = "#272a37", bold = true }

# Symlink
symlink_target = { fg = "#6c7393", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#af9973", bg = "#af9973" }
marker_cut      = { fg = "#b7657a", bg = "#b7657a" }
marker_marked   = { fg = "#b976a7", bg = "#b976a7" }
marker_selected = { fg = "#6076a0", bg = "#6076a0" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#fcfcff", bg = "#5b7e59" }
count_cut      = { fg = "#f9f9ff", bg = "#a85a6d" }
count_selected = { fg = "#f9f9ff", bg = "#577972" }

# Border
border_symbol = "│"
border_style  = { fg = "#506c50" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#2e2f3f" }
current = { bg = "#313346" }
preview = { bg = "#313346" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b2b7c7", bg = "#24252f", bold = true }
inactive = { fg = "#9b9eaa", bg = "#22232d" }

[mode]
normal_main = { fg = "#fcfcff", bg = "#6076a0", bold = true }
normal_alt  = { fg = "#89a5db", bg = "#36384b" }

select_main = { fg = "#1e1f29", bg = "#b976a7", bold = true }
select_alt  = { fg = "#cc91bc", bg = "#36384b" }

unset_main  = { fg = "#ebedff", bg = "#7c609b", bold = true }
unset_alt   = { fg = "#b297d2", bg = "#36384b" }

[status]
# Progress
progress_label  = { fg = "#b2b7c7", bold = true }
progress_normal = { fg = "#b2b7c7", bg = "#1e1f29" }
progress_error  = { fg = "#fcfcff", bg = "#b7657a" }

# Permissions
perm_type  = { fg = "#6c8bc3" }
perm_read  = { fg = "#b8a47e" }
perm_write = { fg = "#c67287" }
perm_exec  = { fg = "#70966e" }
perm_sep   = { fg = "#6c7393" }

[pick]
border   = { fg = "#576d9a" }
active   = { fg = "#b2b7c7", bg = "#24252f" }
inactive = { fg = "#b2b7c7" }

[input]
border   = { fg = "#576d9a" }
title    = { fg = "#576d9a" }
value    = { fg = "#9c7ebe" }
selected = { fg = "#b2b7c7", bg = "#24252f" }

[cmp]
border   = { fg = "#576d9a" }
active   = { fg = "#b2b7c7", bg = "#24252f" }
inactive = { fg = "#b2b7c7" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#506c50" }
title   = { fg = "#b2b7c7" }
hovered = { fg = "#b2b7c7", bg = "#24252f" }

[which]
cols            = 3
mask            = { bg = "#1e1f29" }
cand            = { fg = "#688d94" }
rest            = { fg = "#6a88be" }
desc            = { fg = "#b976a7" }
separator       = " ➜ "
separator_style = { fg = "#6c7393" }

[confirm]
border  = { fg = "#576d9a" }
title   = { fg = "#576d9a" }
body    = {}
list    = {}
btn_yes = { fg = "#b2b7c7", bg = "#24252f" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#506c50" }
title    = { fg = "#b2b7c7" }
tbl_col  = { fg = "#6c8bc3" }
tbl_cell = { fg = "#b8a47e", bg = "#24252f" }

[notify]
title_info  = { fg = "#668bd0" }
title_warn  = { fg = "#b47e65" }
title_error = { fg = "#cd6e85" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#70966e" }
run     = { fg = "#b976a7" }
desc    = { fg = "#9b9eaa" }
hovered = { fg = "#b2b7c7", bg = "#24252f", bold = true }
footer  = { fg = "#b2b7c7", bg = "#24252f" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#b8a47e" },

	# Media
	{ mime = "{audio,video}/*", fg = "#b976a7" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#c67287" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#6b929a" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#6c7393" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#b7657a" },
	{ url = "*", is = "exec"  , fg = "#70966e" },
	{ url = "*", is = "dummy", bg = "#b7657a" },
	{ url = "*/", is = "dummy", bg = "#b7657a" },

	# Fallback
	{ url = "*/", fg = "#6c8bc3" }
]
    