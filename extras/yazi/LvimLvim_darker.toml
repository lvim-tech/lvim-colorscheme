#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme LvimDarker
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#619191", italic = true }

# Find
find_keyword  = { fg = "#141819", bg = "#FF9C2A", bold = true }
find_position = { fg = "#55a6ab", bg = "#1a2a31", bold = true }

# Symlink
symlink_target = { fg = "#567273", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#eab749", bg = "#eab749" }
marker_cut      = { fg = "#e65e4f", bg = "#e65e4f" }
marker_marked   = { fg = "#c75940", bg = "#c75940" }
marker_selected = { fg = "#0A85B9", bg = "#0A85B9" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#141819", bg = "#7d9654" }
count_cut      = { fg = "#141819", bg = "#e34a39" }
count_selected = { fg = "#9fefff", bg = "#1c7060" }

# Border
border_symbol = "│"
border_style  = { fg = "#6a7f4b" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#272f34" }
current = { bg = "#2a3439" }
preview = { bg = "#2a3439" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#55a6ab", bg = "#272f34", bold = true }
inactive = { fg = "#3a8287", bg = "#1c2225" }

[mode]
normal_main = { fg = "#feffff", bg = "#0A85B9", bold = true }
normal_alt  = { fg = "#00aff2", bg = "#323c43" }

select_main = { fg = "#feffff", bg = "#c75940", bold = true }
select_alt  = { fg = "#fb816a", bg = "#323c43" }

unset_main  = { fg = "#f6fdff", bg = "#177ab3", bold = true }
unset_alt   = { fg = "#0eaeff", bg = "#323c43" }

[status]
# Progress
progress_label  = { fg = "#619191", bold = true }
progress_normal = { fg = "#55a6ab", bg = "#141819" }
progress_error  = { fg = "#141819", bg = "#e65e4f" }

# Permissions
perm_type  = { fg = "#0093cc" }
perm_read  = { fg = "#edc060" }
perm_write = { fg = "#e65e4f" }
perm_exec  = { fg = "#8AA55F" }
perm_sep   = { fg = "#567273" }

[pick]
border   = { fg = "#0974a1" }
active   = { fg = "#55a6ab", bg = "#272f34" }
inactive = { fg = "#619191" }

[input]
border   = { fg = "#0974a1" }
title    = { fg = "#0974a1" }
value    = { fg = "#0090d4" }
selected = { fg = "#55a6ab", bg = "#272f34" }

[cmp]
border   = { fg = "#0974a1" }
active   = { fg = "#55a6ab", bg = "#272f34" }
inactive = { fg = "#619191" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#6a7f4b" }
title   = { fg = "#619191" }
hovered = { fg = "#55a6ab", bg = "#272f34" }

[which]
cols            = 3
mask            = { bg = "#141819" }
cand            = { fg = "#018FA6" }
rest            = { fg = "#008bc1" }
desc            = { fg = "#d65c3f" }
separator       = " ➜ "
separator_style = { fg = "#526a6b" }

[confirm]
border  = { fg = "#0974a1" }
title   = { fg = "#0974a1" }
body    = {}
list    = {}
btn_yes = { fg = "#55a6ab", bg = "#272f34" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#6a7f4b" }
title    = { fg = "#619191" }
tbl_col  = { fg = "#0093cc" }
tbl_cell = { fg = "#edc060", bg = "#272f34" }

[notify]
title_info  = { fg = "#0092ca" }
title_warn  = { fg = "#ff9010" }
title_error = { fg = "#f24d3a" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#8AA55F" }
run     = { fg = "#e05f3e" }
desc    = { fg = "#3a8287" }
hovered = { fg = "#55a6ab", bg = "#272f34", bold = true }
footer  = { fg = "#55a6ab", bg = "#272f34" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#edc060" },

	# Media
	{ mime = "{audio,video}/*", fg = "#e05f3e" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#e65e4f" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#0095ac" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#567273" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#e65e4f" },
	{ url = "*", is = "exec"  , fg = "#8AA55F" },
	{ url = "*", is = "dummy", bg = "#e65e4f" },
	{ url = "*/", is = "dummy", bg = "#e65e4f" },

	# Fallback
	{ url = "*/", fg = "#0093cc" }
]
    