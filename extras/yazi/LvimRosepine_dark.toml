#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme RosepineDark
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b4b2c7", italic = true }

# Find
find_keyword  = { fg = "#100f1a", bg = "#c68784", bold = true }
find_position = { fg = "#b8b5c4", bg = "#1c1f2c", bold = true }

# Symlink
symlink_target = { fg = "#746e91", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#d09a4f", bg = "#d09a4f" }
marker_cut      = { fg = "#cd6985", bg = "#cd6985" }
marker_marked   = { fg = "#9f80c4", bg = "#9f80c4" }
marker_selected = { fg = "#416d7f", bg = "#416d7f" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#100f1a", bg = "#71a7b0" }
count_cut      = { fg = "#fefeff", bg = "#c65474" }
count_selected = { fg = "#d9d7e0", bg = "#3b6374" }

# Border
border_symbol = "│"
border_style  = { fg = "#5f8a94" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#282535" }
current = { bg = "#2c293c" }
preview = { bg = "#2c293c" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b8b5c4", bg = "#282535", bold = true }
inactive = { fg = "#9694a3", bg = "#191724" }

[mode]
normal_main = { fg = "#e7e5ff", bg = "#416d7f", bold = true }
normal_alt  = { fg = "#27aacf", bg = "#363345" }

select_main = { fg = "#100f1a", bg = "#9f80c4", bold = true }
select_alt  = { fg = "#ae91d1", bg = "#363345" }

unset_main  = { fg = "#100f1a", bg = "#958fa7", bold = true }
unset_alt   = { fg = "#9f9ab1", bg = "#363345" }

[status]
# Progress
progress_label  = { fg = "#b4b2c7", bold = true }
progress_normal = { fg = "#b8b5c4", bg = "#100f1a" }
progress_error  = { fg = "#100f1a", bg = "#cd6985" }

# Permissions
perm_type  = { fg = "#3f8ca7" }
perm_read  = { fg = "#d6a765" }
perm_write = { fg = "#cd6985" }
perm_exec  = { fg = "#83b2ba" }
perm_sep   = { fg = "#746e91" }

[pick]
border   = { fg = "#3c6b7f" }
active   = { fg = "#b8b5c4", bg = "#282535" }
inactive = { fg = "#b4b2c7" }

[input]
border   = { fg = "#3c6b7f" }
title    = { fg = "#3c6b7f" }
value    = { fg = "#958fa7" }
selected = { fg = "#b8b5c4", bg = "#282535" }

[cmp]
border   = { fg = "#3c6b7f" }
active   = { fg = "#b8b5c4", bg = "#282535" }
inactive = { fg = "#b4b2c7" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#5f8a94" }
title   = { fg = "#b4b2c7" }
hovered = { fg = "#b8b5c4", bg = "#282535" }

[which]
cols            = 3
mask            = { bg = "#100f1a" }
cand            = { fg = "#83b2ba" }
rest            = { fg = "#4086a0" }
desc            = { fg = "#9f80c4" }
separator       = " ➜ "
separator_style = { fg = "#746e91" }

[confirm]
border  = { fg = "#3c6b7f" }
title   = { fg = "#3c6b7f" }
body    = {}
list    = {}
btn_yes = { fg = "#b8b5c4", bg = "#282535" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#5f8a94" }
title    = { fg = "#b4b2c7" }
tbl_col  = { fg = "#3f8ca7" }
tbl_cell = { fg = "#d6a765", bg = "#282535" }

[notify]
title_info  = { fg = "#328dac" }
title_warn  = { fg = "#bd7370" }
title_error = { fg = "#d5567b" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#83b2ba" }
run     = { fg = "#9f80c4" }
desc    = { fg = "#9694a3" }
hovered = { fg = "#b8b5c4", bg = "#282535", bold = true }
footer  = { fg = "#b8b5c4", bg = "#282535" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#d6a765" },

	# Media
	{ mime = "{audio,video}/*", fg = "#9f80c4" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#cd6985" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#83b2ba" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#746e91" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#cd6985" },
	{ url = "*", is = "exec"  , fg = "#83b2ba" },
	{ url = "*", is = "dummy", bg = "#cd6985" },
	{ url = "*/", is = "dummy", bg = "#cd6985" },

	# Fallback
	{ url = "*/", fg = "#3f8ca7" }
]
    