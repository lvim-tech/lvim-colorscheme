#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme RosepineDark
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b3afb2", italic = true }

# Find
find_keyword  = { fg = "#19171b", bg = "#a98a89", bold = true }
find_position = { fg = "#b9b4b5", bg = "#222226", bold = true }

# Symlink
symlink_target = { fg = "#896882", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#ac9682", bg = "#ac9682" }
marker_cut      = { fg = "#a0727c", bg = "#a0727c" }
marker_marked   = { fg = "#9084a2", bg = "#9084a2" }
marker_selected = { fg = "#5d6d74", bg = "#5d6d74" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#fdfcff", bg = "#697d6d" }
count_cut      = { fg = "#faf5ff", bg = "#95646f" }
count_selected = { fg = "#dbd9d9", bg = "#51635d" }

# Border
border_symbol = "│"
border_style  = { fg = "#5a695d" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#2a272e" }
current = { bg = "#312d36" }
preview = { bg = "#312d36" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b9b4b5", bg = "#1f1d21", bold = true }
inactive = { fg = "#979295", bg = "#1d1b1f" }

[mode]
normal_main = { fg = "#f2e7fd", bg = "#5d6d74", bold = true }
normal_alt  = { fg = "#7fa0ae", bg = "#36313b" }

select_main = { fg = "#19171b", bg = "#9084a2", bold = true }
select_alt  = { fg = "#a297b3", bg = "#36313b" }

unset_main  = { fg = "#faf5ff", bg = "#726f7c", bold = true }
unset_alt   = { fg = "#9e9ba8", bg = "#36313b" }

[status]
# Progress
progress_label  = { fg = "#b3afb2", bold = true }
progress_normal = { fg = "#b9b4b5", bg = "#19171b" }
progress_error  = { fg = "#fdfcff", bg = "#a0727c" }

# Permissions
perm_type  = { fg = "#6f8792" }
perm_read  = { fg = "#b6a18b" }
perm_write = { fg = "#a77681" }
perm_exec  = { fg = "#788b7c" }
perm_sep   = { fg = "#896882" }

[pick]
border   = { fg = "#596b72" }
active   = { fg = "#b9b4b5", bg = "#1f1d21" }
inactive = { fg = "#b3afb2" }

[input]
border   = { fg = "#596b72" }
title    = { fg = "#596b72" }
value    = { fg = "#868391" }
selected = { fg = "#b9b4b5", bg = "#1f1d21" }

[cmp]
border   = { fg = "#596b72" }
active   = { fg = "#b9b4b5", bg = "#1f1d21" }
inactive = { fg = "#b3afb2" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#5a695d" }
title   = { fg = "#b3afb2" }
hovered = { fg = "#b9b4b5", bg = "#1f1d21" }

[which]
cols            = 3
mask            = { bg = "#19171b" }
cand            = { fg = "#788787" }
rest            = { fg = "#6d858f" }
desc            = { fg = "#9084a2" }
separator       = " ➜ "
separator_style = { fg = "#896882" }

[confirm]
border  = { fg = "#596b72" }
title   = { fg = "#596b72" }
body    = {}
list    = {}
btn_yes = { fg = "#b9b4b5", bg = "#1f1d21" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#5a695d" }
title    = { fg = "#b3afb2" }
tbl_col  = { fg = "#6f8792" }
tbl_cell = { fg = "#b6a18b", bg = "#1f1d21" }

[notify]
title_info  = { fg = "#6c8993" }
title_warn  = { fg = "#a07b7a" }
title_error = { fg = "#b07280" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#788b7c" }
run     = { fg = "#9084a2" }
desc    = { fg = "#979295" }
hovered = { fg = "#b9b4b5", bg = "#1f1d21", bold = true }
footer  = { fg = "#b9b4b5", bg = "#1f1d21" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#b6a18b" },

	# Media
	{ mime = "{audio,video}/*", fg = "#9084a2" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#a77681" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#788787" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#896882" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#a0727c" },
	{ url = "*", is = "exec"  , fg = "#788b7c" },
	{ url = "*", is = "dummy", bg = "#a0727c" },
	{ url = "*/", is = "dummy", bg = "#a0727c" },

	# Fallback
	{ url = "*/", fg = "#6f8792" }
]
    