#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme NightfoxDark
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#bcbdbd", italic = true }

# Find
find_keyword  = { fg = "#131a24", bg = "#d49360", bold = true }
find_position = { fg = "#bcbcbd", bg = "#202d3d", bold = true }

# Symlink
symlink_target = { fg = "#758292", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#b9a15e", bg = "#b9a15e" }
marker_cut      = { fg = "#b46478", bg = "#b46478" }
marker_marked   = { fg = "#9b82af", bg = "#9b82af" }
marker_selected = { fg = "#7292bd", bg = "#7292bd" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#131a24", bg = "#799e8c" }
count_cut      = { fg = "#eef4ff", bg = "#ac5369" }
count_selected = { fg = "#131a24", bg = "#62b2b4" }

# Border
border_symbol = "│"
border_style  = { fg = "#66857a" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#212e40" }
current = { bg = "#23354c" }
preview = { bg = "#23354c" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#bcbcbd", bg = "#212e40", bold = true }
inactive = { fg = "#b1b5bc", bg = "#192330" }

[mode]
normal_main = { fg = "#131a24", bg = "#7292bd", bold = true }
normal_alt  = { fg = "#82a5d3", bg = "#29394f" }

select_main = { fg = "#131a24", bg = "#9b82af", bold = true }
select_alt  = { fg = "#af98c2", bg = "#29394f" }

unset_main  = { fg = "#131a24", bg = "#9380b2", bold = true }
unset_alt   = { fg = "#ac9bc9", bg = "#29394f" }

[status]
# Progress
progress_label  = { fg = "#bcbdbd", bold = true }
progress_normal = { fg = "#bcbcbd", bg = "#131a24" }
progress_error  = { fg = "#fbfcff", bg = "#b46478" }

# Permissions
perm_type  = { fg = "#7292bd" }
perm_read  = { fg = "#c1ac71" }
perm_write = { fg = "#c47085" }
perm_exec  = { fg = "#89aa9a" }
perm_sep   = { fg = "#758292" }

[pick]
border   = { fg = "#5f84b4" }
active   = { fg = "#bcbcbd", bg = "#212e40" }
inactive = { fg = "#bcbdbd" }

[input]
border   = { fg = "#5f84b4" }
title    = { fg = "#5f84b4" }
value    = { fg = "#9380b2" }
selected = { fg = "#bcbcbd", bg = "#212e40" }

[cmp]
border   = { fg = "#5f84b4" }
active   = { fg = "#bcbcbd", bg = "#212e40" }
inactive = { fg = "#bcbdbd" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#66857a" }
title   = { fg = "#bcbdbd" }
hovered = { fg = "#bcbcbd", bg = "#212e40" }

[which]
cols            = 3
mask            = { bg = "#131a24" }
cand            = { fg = "#75bbbd" }
rest            = { fg = "#7292bd" }
desc            = { fg = "#9b82af" }
separator       = " ➜ "
separator_style = { fg = "#758292" }

[confirm]
border  = { fg = "#5f84b4" }
title   = { fg = "#5f84b4" }
body    = {}
list    = {}
btn_yes = { fg = "#bcbcbd", bg = "#212e40" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#66857a" }
title    = { fg = "#bcbdbd" }
tbl_col  = { fg = "#7292bd" }
tbl_cell = { fg = "#c1ac71", bg = "#212e40" }

[notify]
title_info  = { fg = "#628cc1" }
title_warn  = { fg = "#ce844a" }
title_error = { fg = "#d56481" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#89aa9a" }
run     = { fg = "#9b82af" }
desc    = { fg = "#b1b5bc" }
hovered = { fg = "#bcbcbd", bg = "#212e40", bold = true }
footer  = { fg = "#bcbcbd", bg = "#212e40" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#c1ac71" },

	# Media
	{ mime = "{audio,video}/*", fg = "#9b82af" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#c47085" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#75bbbd" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#758292" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#b46478" },
	{ url = "*", is = "exec"  , fg = "#89aa9a" },
	{ url = "*", is = "dummy", bg = "#b46478" },
	{ url = "*/", is = "dummy", bg = "#b46478" },

	# Fallback
	{ url = "*/", fg = "#7292bd" }
]
    