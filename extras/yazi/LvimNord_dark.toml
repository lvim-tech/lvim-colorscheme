#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme NordDark
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b3bac6", italic = true }

# Find
find_keyword  = { fg = "#1a1e25", bg = "#ba8574", bold = true }
find_position = { fg = "#b4bac6", bg = "#2b323d", bold = true }

# Symlink
symlink_target = { fg = "#677185", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#c3a25d", bg = "#c3a25d" }
marker_cut      = { fg = "#af7177", bg = "#af7177" }
marker_marked   = { fg = "#a58aa0", bg = "#a58aa0" }
marker_selected = { fg = "#8097af", bg = "#8097af" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#1a1e25", bg = "#89a076" }
count_cut      = { fg = "#fbfdff", bg = "#a66067" }
count_selected = { fg = "#1a1e25", bg = "#789f9d" }

# Border
border_symbol = "│"
border_style  = { fg = "#758868" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#2d333f" }
current = { bg = "#303846" }
preview = { bg = "#303846" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b4bac6", bg = "#2b313c", bold = true }
inactive = { fg = "#b0b3ba", bg = "#232831" }

[mode]
normal_main = { fg = "#1a1e25", bg = "#8097af", bold = true }
normal_alt  = { fg = "#90adca", bg = "#353e4c" }

select_main = { fg = "#1a1e25", bg = "#a58aa0", bold = true }
select_alt  = { fg = "#bba3b6", bg = "#353e4c" }

unset_main  = { fg = "#1a1e25", bg = "#9b93a4", bold = true }
unset_alt   = { fg = "#ada6b5", bg = "#353e4c" }

[status]
# Progress
progress_label  = { fg = "#b3bac6", bold = true }
progress_normal = { fg = "#b4bac6", bg = "#1a1e25" }
progress_error  = { fg = "#fbfdff", bg = "#af7177" }

# Permissions
perm_type  = { fg = "#8097af" }
perm_read  = { fg = "#cbae72" }
perm_write = { fg = "#b88085" }
perm_exec  = { fg = "#97ab86" }
perm_sep   = { fg = "#677185" }

[pick]
border   = { fg = "#6f89a4" }
active   = { fg = "#b4bac6", bg = "#2b313c" }
inactive = { fg = "#b3bac6" }

[input]
border   = { fg = "#6f89a4" }
title    = { fg = "#6f89a4" }
value    = { fg = "#9b93a4" }
selected = { fg = "#b4bac6", bg = "#2b313c" }

[cmp]
border   = { fg = "#6f89a4" }
active   = { fg = "#b4bac6", bg = "#2b313c" }
inactive = { fg = "#b3bac6" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#758868" }
title   = { fg = "#b3bac6" }
hovered = { fg = "#b4bac6", bg = "#2b313c" }

[which]
cols            = 3
mask            = { bg = "#1a1e25" }
cand            = { fg = "#7eaab7" }
rest            = { fg = "#8097af" }
desc            = { fg = "#a58aa0" }
separator       = " ➜ "
separator_style = { fg = "#677185" }

[confirm]
border  = { fg = "#6f89a4" }
title   = { fg = "#6f89a4" }
body    = {}
list    = {}
btn_yes = { fg = "#b4bac6", bg = "#2b313c" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#758868" }
title    = { fg = "#b3bac6" }
tbl_col  = { fg = "#8097af" }
tbl_cell = { fg = "#cbae72", bg = "#2b313c" }

[notify]
title_info  = { fg = "#7491af" }
title_warn  = { fg = "#c17d67" }
title_error = { fg = "#c67b83" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#97ab86" }
run     = { fg = "#a58aa0" }
desc    = { fg = "#b0b3ba" }
hovered = { fg = "#b4bac6", bg = "#2b313c", bold = true }
footer  = { fg = "#b4bac6", bg = "#2b313c" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#cbae72" },

	# Media
	{ mime = "{audio,video}/*", fg = "#a58aa0" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#b88085" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#7eaab7" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#677185" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#af7177" },
	{ url = "*", is = "exec"  , fg = "#97ab86" },
	{ url = "*", is = "dummy", bg = "#af7177" },
	{ url = "*/", is = "dummy", bg = "#af7177" },

	# Fallback
	{ url = "*/", fg = "#8097af" }
]
    