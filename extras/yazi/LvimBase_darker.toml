#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme BaseDarker
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#619191", italic = true }

# Find
find_keyword  = { fg = "#181e21", bg = "#e19556", bold = true }
find_position = { fg = "#55a6ab", bg = "#1f2a2f", bold = true }

# Symlink
symlink_target = { fg = "#567273", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#d1aa64", bg = "#d1aa64" }
marker_cut      = { fg = "#d75e52", bg = "#d75e52" }
marker_marked   = { fg = "#c6614d", bg = "#c6614d" }
marker_selected = { fg = "#447ca2", bg = "#447ca2" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#feffff", bg = "#718650" }
count_cut      = { fg = "#feffff", bg = "#cc5147" }
count_selected = { fg = "#b2e7ff", bg = "#366c5f" }

# Border
border_symbol = "│"
border_style  = { fg = "#607247" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#252f34" }
current = { bg = "#27343a" }
preview = { bg = "#27343a" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#55a6ab", bg = "#1e2427", bold = true }
inactive = { fg = "#3a8287", bg = "#1c2225" }

[mode]
normal_main = { fg = "#feffff", bg = "#447ca2", bold = true }
normal_alt  = { fg = "#1fadef", bg = "#2c3b41" }

select_main = { fg = "#feffff", bg = "#c6614d", bold = true }
select_alt  = { fg = "#f08673", bg = "#2c3b41" }

unset_main  = { fg = "#def4ff", bg = "#40719a", bold = true }
unset_alt   = { fg = "#00acfd", bg = "#2c3b41" }

[status]
# Progress
progress_label  = { fg = "#619191", bold = true }
progress_normal = { fg = "#55a6ab", bg = "#181e21" }
progress_error  = { fg = "#181e21", bg = "#d75e52" }

# Permissions
perm_type  = { fg = "#4090c0" }
perm_read  = { fg = "#d8b36f" }
perm_write = { fg = "#df6255" }
perm_exec  = { fg = "#7e965a" }
perm_sep   = { fg = "#567273" }

[pick]
border   = { fg = "#3a7295" }
active   = { fg = "#55a6ab", bg = "#1e2427" }
inactive = { fg = "#619191" }

[input]
border   = { fg = "#3a7295" }
title    = { fg = "#3a7295" }
value    = { fg = "#358fcd" }
selected = { fg = "#55a6ab", bg = "#1e2427" }

[cmp]
border   = { fg = "#3a7295" }
active   = { fg = "#55a6ab", bg = "#1e2427" }
inactive = { fg = "#619191" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#607247" }
title   = { fg = "#619191" }
hovered = { fg = "#55a6ab", bg = "#1e2427" }

[which]
cols            = 3
mask            = { bg = "#181e21" }
cand            = { fg = "#4291a3" }
rest            = { fg = "#418dbc" }
desc            = { fg = "#d0644e" }
separator       = " ➜ "
separator_style = { fg = "#536d6e" }

[confirm]
border  = { fg = "#3a7295" }
title   = { fg = "#3a7295" }
body    = {}
list    = {}
btn_yes = { fg = "#55a6ab", bg = "#1e2427" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#607247" }
title    = { fg = "#619191" }
tbl_col  = { fg = "#4090c0" }
tbl_cell = { fg = "#d8b36f", bg = "#1e2427" }

[notify]
title_info  = { fg = "#2691c5" }
title_warn  = { fg = "#da8950" }
title_error = { fg = "#e8594d" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#7e965a" }
run     = { fg = "#da664e" }
desc    = { fg = "#3a8287" }
hovered = { fg = "#55a6ab", bg = "#1e2427", bold = true }
footer  = { fg = "#55a6ab", bg = "#1e2427" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#d8b36f" },

	# Media
	{ mime = "{audio,video}/*", fg = "#da664e" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#df6255" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#4194a7" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#567273" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#d75e52" },
	{ url = "*", is = "exec"  , fg = "#7e965a" },
	{ url = "*", is = "dummy", bg = "#d75e52" },
	{ url = "*/", is = "dummy", bg = "#d75e52" },

	# Fallback
	{ url = "*/", fg = "#4090c0" }
]
    