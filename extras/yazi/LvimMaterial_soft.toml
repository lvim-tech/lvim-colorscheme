#:schema https://yazi-rs.github.io/schemas/theme.json
# Lvim Colorscheme MaterialSoft
# Generated by lvim-colorscheme for Yazi 26 — https://github.com/sxyazi/yazi

[mgr]
cwd = { fg = "#b2c7c7", italic = true }

# Find
find_keyword  = { fg = "#1f282d", bg = "#d77c61", bold = true }
find_position = { fg = "#b8bec2", bg = "#2a3a49", bold = true }

# Symlink
symlink_target = { fg = "#6e7f87", italic = true }

# Marker — a solid bar, so foreground and background match on purpose
marker_copied   = { fg = "#d9a544", bg = "#d9a544" }
marker_cut      = { fg = "#d1676c", bg = "#d1676c" }
marker_marked   = { fg = "#da596e", bg = "#da596e" }
marker_selected = { fg = "#618adf", bg = "#618adf" }
marker_symbol   = "│"

# Count
count_copied   = { fg = "#1f282d", bg = "#99c15f" }
count_cut      = { fg = "#fcfeff", bg = "#ca5157" }
count_selected = { fg = "#1f282d", bg = "#43d8b5" }

# Border
border_symbol = "│"
border_style  = { fg = "#82a557" }

# The row under the cursor in each pane. In 25 this was `hovered` and
# `preview_hovered` under [manager]; 26 gives the parent pane one of its own.
#
# `padding` is REQUIRED here, and it is required because of the `bg` above.
# components/entity.lua gives every row one leading space, except a hovered row
# whose style has a background — that case is handed `indicator.padding.open`
# instead, and the preset leaves it empty:
#
#   function Entity:padding()
#     if not self._file.is_hovered then return " " end
#     local style = self:style_rev()          -- non-nil only when the row has a bg
#     if style then return ui.Span(th.indicator.padding.open) else return " " end
#   end
#
# Upstream styles the hovered row with `reversed = true` and no background, so
# it never reaches that branch. A wash — which is what this palette wants, and
# what Yazi 25 did here — does reach it, and an empty padding then costs the
# hovered row a column and shifts its icon out of line with every other row.
[indicator]
parent  = { bg = "#2f3d44" }
current = { bg = "#33444d" }
preview = { bg = "#33444d" }
padding = { open = " ", close = " " }

[tabs]
active   = { fg = "#b8bec2", bg = "#2f3d44", bold = true }
inactive = { fg = "#bbc8cd", bg = "#273339" }

[mode]
normal_main = { fg = "#fcfeff", bg = "#618adf", bold = true }
normal_alt  = { fg = "#97b2f8", bg = "#374851" }

select_main = { fg = "#fcfeff", bg = "#da596e", bold = true }
select_alt  = { fg = "#f29ca7", bg = "#374851" }

unset_main  = { fg = "#1f282d", bg = "#9e86b0", bold = true }
unset_alt   = { fg = "#c0add0", bg = "#374851" }

[status]
# Progress
progress_label  = { fg = "#b2c7c7", bold = true }
progress_normal = { fg = "#b8bec2", bg = "#1f282d" }
progress_error  = { fg = "#fcfeff", bg = "#d1676c" }

# Permissions
perm_type  = { fg = "#7297e9" }
perm_read  = { fg = "#deb05c" }
perm_write = { fg = "#dc7f83" }
perm_exec  = { fg = "#a6c974" }
perm_sep   = { fg = "#6e7f87" }

[pick]
border   = { fg = "#4978da" }
active   = { fg = "#b8bec2", bg = "#2f3d44" }
inactive = { fg = "#b2c7c7" }

[input]
border   = { fg = "#4978da" }
title    = { fg = "#4978da" }
value    = { fg = "#a891ba" }
selected = { fg = "#b8bec2", bg = "#2f3d44" }

[cmp]
border   = { fg = "#4978da" }
active   = { fg = "#b8bec2", bg = "#2f3d44" }
inactive = { fg = "#b2c7c7" }

icon_file    = ""
icon_folder  = ""
icon_command = ""

[tasks]
border  = { fg = "#82a557" }
title   = { fg = "#b2c7c7" }
hovered = { fg = "#b8bec2", bg = "#2f3d44" }

[which]
cols            = 3
mask            = { bg = "#1f282d" }
cand            = { fg = "#63bbdf" }
rest            = { fg = "#648de1" }
desc            = { fg = "#e0687b" }
separator       = " ➜ "
separator_style = { fg = "#647278" }

[confirm]
border  = { fg = "#4978da" }
title   = { fg = "#4978da" }
body    = {}
list    = {}
btn_yes = { fg = "#b8bec2", bg = "#2f3d44" }
btn_no  = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

[spot]
border   = { fg = "#82a557" }
title    = { fg = "#b2c7c7" }
tbl_col  = { fg = "#7297e9" }
tbl_cell = { fg = "#deb05c", bg = "#2f3d44" }

[notify]
title_info  = { fg = "#7097f8" }
title_warn  = { fg = "#ee7753" }
title_error = { fg = "#e77b7f" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

[help]
on      = { fg = "#a6c974" }
run     = { fg = "#e67787" }
desc    = { fg = "#bbc8cd" }
hovered = { fg = "#b8bec2", bg = "#2f3d44", bold = true }
footer  = { fg = "#b8bec2", bg = "#2f3d44" }

[filetype]
rules = [
	# Images
	{ mime = "image/*", fg = "#deb05c" },

	# Media
	{ mime = "{audio,video}/*", fg = "#e67787" },

	# Archives
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#dc7f83" },

	# Documents
	{ mime = "application/{pdf,doc,rtf}", fg = "#63bbdf" },

	# Virtual file system — 26 reports unreachable remote entries through this
	{ mime = "vfs/{absent,stale}", fg = "#6e7f87" },

	# Special files. `url` here was `name` before 26.
	{ url = "*", is = "orphan", bg = "#d1676c" },
	{ url = "*", is = "exec"  , fg = "#a6c974" },
	{ url = "*", is = "dummy", bg = "#d1676c" },
	{ url = "*/", is = "dummy", bg = "#d1676c" },

	# Fallback
	{ url = "*/", fg = "#7297e9" }
]
    